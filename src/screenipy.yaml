llm:
  provider: openai           # openai | anthropic | openai-compatible
  model: gpt-4o
  base_url: null             # For openai-compatible endpoints (Ollama, LiteLLM, etc.)
  api_key_env: SCREENIPY_API_KEY

kite_mcp:
  url: https://mcp.kite.trade/mcp
  enabled: true

workflow:
  default_mode: classic      # classic | ai

schedule: []
# Example:
# schedule:
#   - cron: "15 9 * * 1-5"
#     agent: swing_trader
#     notify: null
